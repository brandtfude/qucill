// Example of using array: true in a configuration object
const config = {
  data: [1, 2, 3], // This setting indicates that "data" should be an array
  options: {
    array: true, // This setting might indicate that "options" should be an array
    values: ['A', 'B', 'C']
  }
};
